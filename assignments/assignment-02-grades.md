# 作业 2：成绩统计分析器

> 📅 **布置周期**：第 4-5 周  
> 💯 **总分**：20 分  
> 🎯 **对应章节**：第 4-5 章（函数、数据结构）

## 任务概述

从 CSV 文件读取学生成绩数据，进行统计分析并生成报告。这是学生第一次处理外部数据文件，需要理解函数设计和数据结构选择，并处理真实数据中的各种边界情况。

---

## 学习目标

完成本作业后，学生应能够：

1. ✅ 使用类组织代码，实现模块化设计
2. ✅ 选择合适的数据结构存储和处理数据
3. ✅ 处理 CSV 文件读写和编码问题
4. ✅ 实现基本的数据统计功能
5. ✅ 处理真实数据中的边界情况（缺失值、无效数据等）

---

## 功能需求

### 输入数据格式

`grades.csv` 示例：

```csv
学号,姓名,语文,数学,英语
2024001,张三,85,92,78
2024002,李四,76,88,82
2024003,王五,90,95,88
2024004,赵六,,80,75
```

### 类接口设计

```python
import csv
from typing import List, Dict, Optional

class GradeAnalyzer:
    """成绩分析器"""
    
    def __init__(self):
        self.students: List[Dict] = []
    
    def load_csv(self, filepath: str) -> bool:
        """
        加载 CSV 文件
        
        处理要求：
        - 支持 UTF-8 和 GBK 编码
        - 正确解析缺失值（空单元格存为 None，不是 0）
        - 转换数据类型（字符串转数字）
        """
        pass
    
    def get_average(self, subject: str) -> float:
        """
        计算某科目平均分
        
        注意：缺失值不参与计算
        例如：3个学生，只有2个有数学成绩，则用2个成绩计算平均
        """
        pass
    
    def get_distribution(self, subject: str) -> Dict[str, int]:
        """
        获取分数分布
        
        返回: {'优秀': 2, '良好': 5, '及格': 3, '不及格': 1}
        只统计有成绩的学生
        """
        pass
    
    def get_ranking(self, subject: str) -> List[Dict]:
        """
        获取排名，处理并列情况
        
        并列规则：
        - 两个 95 分并列第 1 名
        - 下一个 90 分排第 3 名（跳过第 2 名）
        
        返回: [
            {'rank': 1, 'id': '2024003', 'name': '王五', 'score': 95},
            {'rank': 1, 'id': '2024005', 'name': '钱七', 'score': 95},
            {'rank': 3, 'id': '2024002', 'name': '李四', 'score': 90},
            ...
        ]
        """
        pass
    
    def export_report(self, filepath: str) -> bool:
        """导出分析报告到文本文件"""
        pass
```

### 分数等级标准

| 等级 | 分数范围 |
|-----|---------|
| 优秀 | 90-100 |
| 良好 | 80-89 |
| 及格 | 60-79 |
| 不及格 | 0-59 |

---

## 测试用例设计

### Core 测试（10 分）

| 测试类别 | 测试名称 | 说明 |
|---------|---------|------|
| CSV加载 | `test_core_load_csv_success` | 成功加载 CSV 文件 |
| CSV加载 | `test_core_load_csv_student_data` | 学生数据正确解析 |
| 平均分 | `test_core_average_math` | 数学平均分计算正确 |
| 平均分 | `test_core_average_chinese` | 语文平均分计算正确 |
| 平均分 | `test_core_average_english` | 英语平均分计算正确 |
| 分布统计 | `test_core_distribution_english` | 英语分数分布正确 |
| 分布统计 | `test_core_distribution_math` | 数学分数分布正确 |
| 排名 | `test_core_ranking_basic` | 基本排名功能 |
| 排名 | `test_core_ranking_tie` | 并列排名处理 |
| 导出 | `test_core_export_success` | 成功导出报告文件 |
| 导出 | `test_core_export_content` | 导出报告内容包含科目信息 |

#### 示例测试代码

```python
@pytest.fixture
def sample_csv(tmp_path):
    """创建标准测试数据"""
    content = """学号,姓名,语文,数学,英语
2024001,张三,85,92,78
2024002,李四,76,88,82
2024003,王五,90,95,88"""
    p = tmp_path / "test_grades.csv"
    p.write_text(content, encoding='utf-8')
    return str(p)


def test_core_load_csv_success(sample_csv):
    """测试成功加载 CSV"""
    analyzer = GradeAnalyzer()
    assert analyzer.load_csv(sample_csv) == True
    assert len(analyzer.students) == 3


def test_core_average_math(sample_csv):
    """测试数学平均分"""
    analyzer = GradeAnalyzer()
    analyzer.load_csv(sample_csv)
    # (92 + 88 + 95) / 3 = 91.67
    assert abs(analyzer.get_average("数学") - 91.67) < 0.1


def test_core_ranking_tie(tmp_path):
    """测试并列排名处理"""
    content = """学号,姓名,数学
2024001,A,95
2024002,B,90
2024003,C,95
2024004,D,80"""
    p = tmp_path / "ranking.csv"
    p.write_text(content, encoding='utf-8')
    
    analyzer = GradeAnalyzer()
    analyzer.load_csv(str(p))
    ranking = analyzer.get_ranking("数学")
    
    # 两个 95 分都应该是第 1 名
    top_scores = [r for r in ranking if r['score'] == 95]
    assert len(top_scores) == 2
    assert all(r['rank'] == 1 for r in top_scores)
    
    # 90 分应该是第 3 名（跳过第 2）
    third = [r for r in ranking if r['score'] == 90][0]
    assert third['rank'] == 3
```

### Edge 测试（5 分）

| 测试类别 | 测试名称 | 说明 | AI 常见遗漏 |
|---------|---------|------|-------------|
| 缺失值 | `test_edge_missing_single` | 单个缺失值不参与计算 | 缺失值当作 0 分 |
| 缺失值 | `test_edge_missing_multiple` | 多个缺失值正确处理 | 缺失值当作 0 分 |
| 缺失值 | `test_edge_missing_not_zero` | 缺失值不应被当作 0 分 | 空字符串转为 0 |
| 编码 | `test_edge_encoding_gbk` | 支持 GBK 编码文件 | 只支持 UTF-8 |
| 空文件 | `test_edge_empty_file` | 空文件不崩溃 | 未检查空文件 |
| 空文件 | `test_edge_header_only` | 只有表头的文件 | 未检查数据行 |
| 无效分数 | `test_edge_negative_score` | 识别负分数 | 不校验分数范围 |
| 无效分数 | `test_edge_over_100_score` | 识别超过100的分数 | 不校验分数范围 |

#### 边界测试示例

```python
def test_edge_missing_single(tmp_path):
    """测试单个缺失值"""
    content = """学号,姓名,语文,数学,英语
2024001,张三,85,,78
2024002,李四,76,88,82"""
    p = tmp_path / "missing.csv"
    p.write_text(content, encoding='utf-8')
    
    analyzer = GradeAnalyzer()
    analyzer.load_csv(str(p))
    
    # 数学只有李四有成绩，平均分应该是 88
    avg = analyzer.get_average("数学")
    assert avg == 88.0


def test_edge_missing_not_zero(tmp_path):
    """测试缺失值不应被当作 0 分"""
    content = """学号,姓名,数学
2024001,张三,
2024002,李四,80"""
    p = tmp_path / "missing_zero.csv"
    p.write_text(content, encoding='utf-8')
    
    analyzer = GradeAnalyzer()
    analyzer.load_csv(str(p))
    
    # 如果缺失值被当作 0，平均分会是 40；正确应该是 80
    avg = analyzer.get_average("数学")
    assert avg == 80.0, "缺失值不应参与平均分计算"


def test_edge_encoding_gbk(tmp_path):
    """测试 GBK 编码文件"""
    content = "学号,姓名,语文\n2024001,张三,85"
    p = tmp_path / "gbk.csv"
    p.write_bytes(content.encode('gbk'))
    
    analyzer = GradeAnalyzer()
    try:
        result = analyzer.load_csv(str(p))
        assert result == True
        assert len(analyzer.students) == 1
    except UnicodeDecodeError:
        pytest.fail("应该能处理 GBK 编码")
```

---

## REPORT.md 要求（5 分）

### 必答内容

学生需要填写 `REPORT.md` 文件，包含以下部分：

1. **设计决策**：选择了什么数据结构存储学生成绩？为什么？考虑过其他方案吗？
2. **复杂需求的表达**：如何向 AI 清晰表达并列排名、缺失值处理等需求？
3. **遇到的挑战**：最大的挑战是什么？如何解决的？
4. **代码质量与责任**：AI 代码直接用了吗？做了什么修改？如何确保正确性？
5. **反思与成长**：AI 擅长什么？不擅长什么？程序员的价值在哪里？

### 评分标准

| 评分维度 | 分值 | 说明 |
|---------|------|------|
| 设计决策的思考深度 | 0-2 分 | 0=未回答；1=有选择但理由简单；2=详细分析优缺点 |
| 复杂需求的表达与沟通 | 0-1 分 | 0=未描述；1=展示了表达复杂需求的策略 |
| 代码质量意识与责任感 | 0-1 分 | 0=未体现；1=展示了验证代码、确保质量的意识 |
| 反思与成长 | 0-1 分 | 0=空白或泛泛而谈；1=有具体反思 |

---

## 提供的测试数据

### `grades_normal.csv`（正常数据）

```csv
学号,姓名,语文,数学,英语
2024001,张三,85,92,78
2024002,李四,76,88,82
2024003,王五,90,95,88
2024004,赵六,68,72,65
2024005,钱七,92,95,90
```

### `grades_edge.csv`（边界数据，供自测）

```csv
学号,姓名,语文,数学,英语
2024001,张三,85,,78
2024002,李四,,88,
2024003,王五,90,95,88
2024001,张三重复,70,70,70
2024004,赵六,-5,110,65
```

---

## 仓库结构

```
assignment-02-grades/
├── src/
│   ├── __init__.py
│   └── grade_analyzer.py    # 学生在此实现
├── tests/
│   └── test_public.py       # 公开测试（学生可见）
├── grades_normal.csv         # 测试数据
├── README.md                 # 作业说明
├── REPORT.md                 # 反思报告（学生填写）
└── requirements.txt
```

---

## Prompt 建议

给学生的提示：

```
请帮我实现一个成绩统计分析器类 GradeAnalyzer，功能如下：

1. load_csv(filepath): 从 CSV 文件加载学生成绩数据
   - 需要处理 UTF-8 和 GBK 编码
   - 缺失值（空单元格）应该存为 None，而不是 0
   
2. get_average(subject): 计算某科目平均分
   - 缺失值不参与计算
   - 例如 3 个学生只有 2 个有数学成绩，就用 2 个计算
   
3. get_distribution(subject): 生成分数分布统计
   - 返回 {'优秀': 2, '良好': 3, '及格': 1, '不及格': 0}
   - 优秀 90-100，良好 80-89，及格 60-79，不及格 0-59
   
4. get_ranking(subject): 生成排名
   - 需要处理并列情况：两个 95 分都排第 1，下一个 90 分排第 3
   - 返回 [{'rank': 1, 'id': '...', 'name': '...', 'score': 95}, ...]
   
5. export_report(filepath): 导出分析报告到文本文件

数据结构建议用 list of dict，每个学生是一个字典。
请提供完整代码，并解释并列排名的实现逻辑。
```

---

## 评分权重

```
┌─────────────────────────────────────┐
│  core 测试    ██████████ 10 分      │
│  edge 测试    █████░░░░░  5 分      │
│  REPORT.md    █████░░░░░  5 分      │
├─────────────────────────────────────┤
│  总分                     20 分     │
└─────────────────────────────────────┘
```

---

## 关键知识点

- **类设计**：使用类组织相关功能，`__init__` 初始化数据存储
- **数据结构选择**：`List[Dict]` vs `Dict[str, Dict]`，各有优缺点
- **文件处理**：CSV 读写、编码检测与回退策略
- **边界处理**：缺失值、无效值、空文件等异常情况
- **并列排名算法**：标准竞赛排名（1, 1, 3, 4...）的实现

---

## 常见问题 FAQ

**Q: 缺失值应该怎么存储？**  
A: 建议存储为 `None`。不要存储为空字符串或 0，因为 0 可能是有效成绩。

**Q: 如何处理编码问题？**  
A: 建议先尝试 UTF-8 解码，如果失败（`UnicodeDecodeError`）再尝试 GBK。

**Q: 并列排名具体怎么实现？**  
A: 标准竞赛排名规则：先按分数排序，相同分数给相同排名，下一个不同分数的排名 = 之前的人数 + 1。

**Q: `invalid_count` 属性是必须的吗？**  
A: 不是必须的，但如果你想追踪无效数据数量，可以添加这个属性。边界测试会检查是否有这个属性。

**Q: 可以用 pandas 吗？**  
A: 基础实现建议只用标准库 `csv` 模块，这样能更好理解底层逻辑。如果熟练后可以用 pandas 简化。
