# 作业 1：个人信息卡生成器

> 📅 **布置周期**：第 2-3 周  
> 💯 **总分**：15 分  
> 🎯 **对应章节**：第 1-3 章（环境、计算思维、调试）

## 任务概述

用 Python 生成格式化的个人信息卡（带边框的文本卡片），需处理多种输入格式和边界情况。这是学生的第一个正式作业，目的是让他们体验完整的 AI 协同开发流程。

---

## 学习目标

完成本作业后，学生应能够：

1. ✅ 使用 Cursor AI 生成基础代码
2. ✅ 理解字符串格式化和基本数据处理
3. ✅ 处理用户输入的各种边界情况
4. ✅ 编写有意义的反思报告

---

## 功能需求

### 核心功能

生成一个格式化的个人信息卡，包含以下字段：

```
┌────────────────────────────────┐
│         个人信息卡              │
├────────────────────────────────┤
│ 姓名：张三                      │
│ 邮箱：zhangsan@example.com     │
│ 手机：138-1234-5678            │
│ 学校：上海应用技术大学          │
│ 专业：经济与管理学院            │
└────────────────────────────────┘
```

### 函数签名

```python
from typing import Optional, Union, Sequence

def generate_card(
    name: str,
    email: str,
    phone: Union[str, Sequence[str]],
    school: Optional[str] = None,
    major: Optional[str] = None,
) -> str:
    """
    生成个人信息卡
    
    Args:
        name: 姓名（必填）
        email: 邮箱（必填，需要基本格式校验）
        phone: 手机号（必填，可以是字符串或列表）
        school: 学校（可选）
        major: 专业（可选）
    
    Returns:
        str: 格式化的信息卡文本
    
    Raises:
        ValueError: 当邮箱格式无效时
    """
    pass
```

### 详细要求

1. **边框结构**：
   - 顶部边框以 `┌` 开始，以 `┐` 结束
   - 底部边框以 `└` 开始，以 `┘` 结束
   - 包含标题"个人信息卡"

2. **手机号标准化**：
   - 无论输入格式如何（纯数字、带空格、带横线），统一输出为 `138-1234-5678` 格式
   - 支持多个手机号（以列表形式传入），用分隔符分隔

3. **邮箱校验**：
   - 检查是否包含 `@` 和 `.`
   - 无效邮箱应抛出 `ValueError` 异常

4. **可选字段处理**：
   - 学校、专业为空时显示"未填写"

5. **宽度控制**：
   - 超长内容需要处理，每行不应超过合理宽度

---

## 测试用例设计

### Core 测试（8 分）

| 测试类别 | 测试名称 | 说明 |
|---------|---------|------|
| 布局测试 | `test_card_basic_layout` | 卡片包含所有必要元素（标题、各字段） |
| 布局测试 | `test_card_border_structure` | 边框结构正确（┌┐└┘） |
| 中文支持 | `test_card_chinese_name` | 正确显示中文姓名 |
| 中文支持 | `test_card_long_chinese_name` | 较长中文姓名正确显示 |
| 邮箱校验 | `test_card_email_valid` | 有效邮箱正常显示 |
| 邮箱校验 | `test_card_email_invalid_no_at` | 缺少@的邮箱抛出异常 |
| 邮箱校验 | `test_card_email_invalid_no_dot` | 缺少.的邮箱抛出异常 |
| 手机号 | `test_card_phone_plain` | 纯数字手机号标准化 |
| 手机号 | `test_card_phone_with_spaces` | 带空格手机号标准化 |
| 手机号 | `test_card_phone_with_dashes` | 带横线手机号标准化 |
| 手机号 | `test_card_phone_consistency` | 不同格式输入产生一致输出 |

#### 示例测试代码

```python
def test_card_basic_layout():
    """测试卡片包含所有必要元素"""
    card = generate_card(
        name="张三",
        email="zhangsan@example.com",
        phone="13812345678",
        school="上海应用技术大学",
        major="经济与管理学院",
    )
    assert "个人信息卡" in card
    assert "张三" in card
    assert "zhangsan@example.com" in card
    assert "上海应用技术大学" in card
    assert "经济与管理学院" in card

def test_card_border_structure():
    """测试卡片边框结构"""
    card = generate_card(
        name="李四",
        email="lisi@test.com",
        phone="13900001111",
    )
    lines = card.strip().splitlines()
    # 顶部边框
    assert lines[0].startswith("┌")
    assert lines[0].endswith("┐")
    # 底部边框
    assert lines[-1].startswith("└")
    assert lines[-1].endswith("┘")

def test_card_phone_consistency():
    """测试不同格式输入产生一致输出"""
    card1 = generate_card("张三", "test@test.com", "138 1234 5678")
    card2 = generate_card("张三", "test@test.com", "138-1234-5678")
    card3 = generate_card("张三", "test@test.com", "13812345678")
    # 所有格式都应该输出 138-1234-5678
    assert "138-1234-5678" in card1
    assert "138-1234-5678" in card2
    assert "138-1234-5678" in card3
```

### Edge 测试（4 分）

| 测试类别 | 测试名称 | 说明 | AI 常见遗漏 |
|---------|---------|------|-------------|
| 空字段 | `test_card_empty_school` | 学校为空显示"未填写" | 不检查 None |
| 空字段 | `test_card_empty_major` | 专业为空显示"未填写" | 不检查 None |
| 空字段 | `test_card_both_optional_empty` | 两个可选字段都为空 | 不检查 None |
| 超长内容 | `test_card_long_name` | 超长姓名不破坏布局 | 无长度限制 |
| 超长内容 | `test_card_long_email` | 超长邮箱不破坏布局 | 无长度限制 |
| 多手机号 | `test_card_multiple_phones_list` | 支持手机号列表 | 不支持列表 |
| 多手机号 | `test_card_multiple_phones_separator` | 多手机号有分隔符 | 无分隔 |
| 特殊字符 | `test_card_special_chars_in_name` | 姓名含特殊字符 | 直接拼接出错 |
| 特殊字符 | `test_card_unicode_name` | Unicode姓名支持 | 编码问题 |

#### 边界测试示例

```python
def test_card_empty_school():
    """测试学校为空时显示占位符"""
    card = generate_card("张三", "test@test.com", "13812345678", school=None)
    assert "未填写" in card

def test_card_long_name():
    """测试超长姓名不会破坏卡片布局"""
    long_name = "阿" * 50
    card = generate_card(long_name, "test@test.com", "13812345678")
    # 每行不应该太宽
    for line in card.splitlines():
        assert len(line) < 80, f"行太长: {len(line)} 字符"

def test_card_multiple_phones_list():
    """测试传入手机号列表"""
    card = generate_card(
        "张三", 
        "test@test.com", 
        ["13812345678", "13999998888"]
    )
    # 两个手机号都应该出现（标准化格式）
    assert "138-1234-5678" in card
    assert "139-9999-8888" in card
```

---

## REPORT.md 要求（3 分）

### 必答内容

学生需要填写 `REPORT.md` 文件，包含以下部分：

1. **Prompt 策略**：如何向 AI 描述需求？最有效的 prompt 是什么？
2. **协作体验**：与 AI 协作是否顺利？有没有误解？有没有惊喜？
3. **思考与判断**：哪部分是主动修改的？如何验证代码正确性？
4. **学习收获**：对"AI 辅助编程"有什么新认识？

### 评分标准

| 评分维度 | 分值 | 说明 |
|---------|------|------|
| Prompt 策略与协作体验 | 1 分 | 有具体描述，展示真实互动过程 |
| 独立思考与判断能力 | 1 分 | 展示对代码的理解、验证或主动优化 |
| 学习收获与反思深度 | 1 分 | 有具体收获，对 AI 辅助编程有自己的思考 |

---

## 仓库结构

```
assignment-01-infocard/
├── src/
│   ├── __init__.py
│   └── card_generator.py    # 学生在此实现
├── tests/
│   └── test_public.py       # 公开测试（学生可见）
├── README.md                 # 作业说明
├── REPORT.md                 # 反思报告（学生填写）
└── requirements.txt
```

---

## Prompt 建议

给学生的提示：

```
请帮我用 Python 实现一个个人信息卡生成器：

功能要求：
1. 生成带边框的文本卡片，包含标题"个人信息卡"
2. 显示姓名、邮箱、手机、学校、专业等信息
3. 手机号需要标准化为 138-1234-5678 格式（去除空格和横线后重新格式化）
4. 可选字段（学校、专业）如果为空，显示"未填写"
5. 邮箱需要基本格式校验（包含@和.），无效邮箱抛出异常
6. 支持多个手机号（以列表形式传入）
7. 控制卡片宽度，超长内容需要处理

边框使用这些字符：┌ ─ ┐ │ ├ ┤ └ ┘

请提供完整代码并解释关键逻辑。
```

---

## 评分权重

```
┌─────────────────────────────────────┐
│  core 测试    ████████░░  8 分      │
│  edge 测试    ████░░░░░░  4 分      │
│  REPORT.md    ███░░░░░░░  3 分      │
├─────────────────────────────────────┤
│  总分                     15 分     │
└─────────────────────────────────────┘
```

---

## 常见问题 FAQ

**Q: AI 生成的代码直接能通过所有测试吗？**  
A: Core 测试大部分可以，但 Edge 测试需要你理解代码并处理边界情况（如多手机号列表、超长内容等）。

**Q: REPORT.md 可以让 AI 帮忙写吗？**  
A: 可以让 AI 帮你整理思路，但内容必须真实反映你的学习过程。虚假内容会被 LLM 评分识别。

**Q: 手机号格式化的标准输出是什么？**  
A: 必须是 `138-1234-5678` 格式（3-4-4），不接受其他格式。

**Q: 邮箱校验应该多严格？**  
A: 基本检查即可：必须包含 `@` 和 `.`。不需要完整的正则表达式校验。

**Q: 如何处理中英文混合的对齐问题？**  
A: 这是一个思考点。中文字符通常占用两个英文字符的宽度，需要在计算对齐时考虑这一点。
