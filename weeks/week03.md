# 第3周：代码理解与调试思维

> **课时**：2课时 | **阶段**：环境搭建与计算思维

## 🎯 学习目标

- 能够阅读和理解 AI 生成的 Python 代码
- 掌握常见语法结构（变量、条件、循环）
- 学会用 AI 调试代码、定位问题

---

## 📚 课程内容

### 1. Python 核心语法速览（40分钟）

> 💡 **教学理念**：不需要背诵语法，重点是"能看懂"

**变量与数据类型**
```python
name = "张三"          # 字符串
age = 20               # 整数
score = 85.5           # 浮点数
is_passed = True       # 布尔值
grades = [80, 90, 75]  # 列表
info = {"name": "张三", "age": 20}  # 字典
```

**条件判断**
```python
if score >= 60:
    print("及格")
elif score >= 40:
    print("补考")
else:
    print("重修")
```

**循环**
```python
# 遍历列表
for grade in grades:
    print(grade)

# 指定次数
for i in range(5):
    print(i)

# 条件循环
while score < 60:
    score += 10
```

**课堂练习**：给一段 AI 生成的代码，让学生逐行解释

### 2. 如何阅读代码（30分钟）

**阅读策略：从全局到细节**

1. **找入口点**：程序从哪里开始执行？
2. **识别主流程**：核心逻辑是什么？
3. **理解数据流**：数据从哪来、到哪去？
4. **关注边界**：特殊情况如何处理？

**实践案例**：阅读一个成绩统计程序
```python
def analyze_scores(scores):
    if not scores:
        return None
    
    total = sum(scores)
    avg = total / len(scores)
    max_score = max(scores)
    min_score = min(scores)
    passed = [s for s in scores if s >= 60]
    pass_rate = len(passed) / len(scores) * 100
    
    return {
        'avg': round(avg, 2),
        'max': max_score,
        'min': min_score,
        'pass_rate': round(pass_rate, 2)
    }
```

**提问引导**：
- 第2-3行在做什么？（边界处理）
- 第7行的 `[s for s in scores if s >= 60]` 是什么意思？（列表推导式）
- 这个函数有什么问题吗？（没处理无效分数）

### 3. 当 AI 生成错误代码时（25分钟）

**常见问题类型**：

| 类型 | 示例 | 如何发现 |
|-----|------|---------|
| 语法错误 | 缩进错误、括号不匹配 | 运行报错 |
| 逻辑错误 | 边界条件遗漏 | 测试用例失败 |
| 类型错误 | 字符串当数字用 | 运行时报错 |
| 遗漏需求 | 没处理空输入 | 功能不完整 |

**案例分析**：找出这段代码的问题
```python
def calculate_average(numbers):
    total = 0
    for num in numbers:
        total += num
    return total / len(numbers)  # 问题：如果 numbers 为空？
```

### 4. 调试技巧：让 AI 帮你找问题（25分钟）

**Prompt 模板**：

```
这段代码报错了，帮我分析原因：

报错信息：
[粘贴错误信息]

代码：
[粘贴代码]
```

```
这段代码运行结果不对：
- 期望输出：xxx
- 实际输出：xxx
帮我找出问题
```

**课堂练习**：给学生一段有 Bug 的代码，让他们：
1. 先自己运行，观察错误
2. 用 AI 帮忙分析
3. 理解问题原因
4. 修复代码

---

## 📝 课后作业

### 提交作业 1：个人信息卡生成器

**本周完成并提交**：
- 通过公开测试用例（Core Tests）
- 处理边界情况（Edge Tests）
- 完成 REPORT.md

**REPORT.md 要求**：
- 记录 3 个 AI 生成代码后你发现需要修改的地方
- 说明问题是什么、你如何发现的、如何修复的

---

## 🐛 调试练习题

**练习 1**：找出问题
```python
def count_passed(scores):
    count = 0
    for score in scores:
        if score > 60:  # 问题：应该是 >= 60
            count += 1
    return count
```

**练习 2**：修复代码
```python
def format_phone(phone):
    # 去除空格和横线，格式化为 xxx-xxxx-xxxx
    cleaned = phone.replace(" ", "").replace("-", "")
    return f"{cleaned[:3]}-{cleaned[3:7]}-{cleaned[7:]}"
    # 问题：没验证长度是否为11位
```

---

## 💡 教学提示

- 避免让学生"背语法"，重点是"能理解"
- 鼓励学生主动"问 AI"来解释代码
- 强调：发现问题 → 定位原因 → 描述给 AI → 验证修复
- 本周结束时，学生应能独立阅读 50 行以内的 Python 代码
