"""
个人信息卡生成器

你的任务是实现 generate_card 函数，生成格式化的个人信息卡。

功能要求：
1. 生成带边框的文本卡片，包含标题"个人信息卡"
2. 显示姓名、邮箱、手机、学校、专业等信息
3. 手机号标准化：无论输入格式如何，统一显示为 138-1234-5678 格式
4. 邮箱校验：检查是否包含 @ 和 .，无效邮箱应抛出异常
5. 可选字段（学校、专业）为空时显示"未填写"
6. 支持多个手机号（以列表形式传入）
7. 控制卡片宽度，超长内容需要处理（截断或换行）

示例输出：
┌────────────────────────────────┐
│         个人信息卡              │
├────────────────────────────────┤
│ 姓名：张三                      │
│ 邮箱：zhangsan@example.com     │
│ 手机：138-1234-5678            │
│ 学校：上海应用技术大学          │
│ 专业：计算机科学与技术          │
└────────────────────────────────┘

提示：
- 可以让 AI 帮你设计代码结构
- 思考：如何处理中文和英文混合时的对齐问题？
- 思考：邮箱校验应该多严格？基本检查还是正则表达式？
"""

from typing import Optional, Union, Sequence


def generate_card(
    name: str,
    email: str,
    phone: Union[str, Sequence[str]],
    school: Optional[str] = None,
    major: Optional[str] = None,
) -> str:
    """
    生成个人信息卡

    Args:
        name: 姓名（必填）
        email: 邮箱（必填，需要基本格式校验）
        phone: 手机号（必填，可以是字符串或列表）
        school: 学校（可选）
        major: 专业（可选）

    Returns:
        str: 格式化的信息卡文本

    Raises:
        ValueError: 当邮箱格式无效时
    """
    # TODO: 在此实现你的代码
    # 
    # 建议的实现步骤：
    # 1. 校验邮箱格式
    # 2. 标准化手机号
    # 3. 处理可选字段
    # 4. 构建卡片内容
    # 5. 添加边框和格式化
    #
    # 你可以：
    # - 创建辅助函数来组织代码
    # - 向 AI 描述你的需求，让它帮你实现
    # - 先实现基本功能，再处理边界情况
    pass


if __name__ == "__main__":
    # 测试你的实现
    print(
        generate_card(
            name="张三",
            email="zhangsan@example.com",
            phone="13812345678",
            school="上海应用技术大学",
            major="经济与管理学院",
        )
    )
